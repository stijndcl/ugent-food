[tool.pylint.master]
disable=[
    "missing-module-docstring",
    "too-few-public-methods",
]

[tool.pylint.format]
max-line-length = 120

[build-system]
build-backend = "setuptools.build_meta"
requires = [
    "setuptools>=43.0.0",
    "wheel"
]